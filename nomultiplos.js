//Ejercicio no multiplos
        let numero; let contador; //Declarar variables

       numero = prompt("Dame un numero: ");
        num = parseInt(String);

        for (contador = 0; contador < numero; contador++) {
            
            if(contador%3 == 0)
                console.log();
            if(contador%7 == 0)
                console.log();

            else
                console.log(contador);
        }
    


